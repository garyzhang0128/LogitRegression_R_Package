\name{predict}
\alias{predict}

\title{
Predict a data set using Logistic Regression
}

\description{
\code{predict} classifies a new set of observations from a
previously built classifier.  This function will provide either
a vector of new classes, class probability estimates.
}

\usage{
\method{predict}(X, w, threshold = 0.5,\dots)
}

\arguments{
  \item{w}{object generated by \code{logitRegression}.}
  \item{X}{new data set to predict.  This data set must be
    of type \sQuote{matrix} and prediction data set is required
    for this approach.}
  \item{threshold}{threshold of classified probability}
  \item{\dots}{other arguments not used by this function.}
}
\examples{
# import dataset
data("breast_cancer_wisconsin")
df = breast_cancer_wisconsin
# generate input
X = df[0:600, 2:10]
X = as.matrix(X)
X = cbind(X, c(1)) # create bias
colnames(X)[10] = "intercept"
y = df[11] # class
set.seed(1)
w = as.matrix(rnorm(10)) # init w

# solve Logit Regression with Gradient Descent Method
w = logitRegresion(X,w,y,method = "gradDesc",L2 = FALSE,alpha = 1,iter = 100000,lamda = 0.0001,eps = 0.001)

# predict
newdata = as.matrix(df[601:699, 2:10])
predict(newdata,w)
}
